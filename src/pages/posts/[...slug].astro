---
import { getEntry } from 'astro:content';
import PostLayout from '../../layouts/PostLayout.astro';

const { slug } = Astro.params;

const blogPost = await getEntry<any, any>('blog', slug);
const { Content } = await (blogPost as any).render();
---

<PostLayout>
  <Content />
</PostLayout>
